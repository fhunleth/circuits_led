defmodule CircuitsLED.SysClass do


  def parse_triggers(trigger_string) do
      trigger_string
      |> String.split(" ")
      |>
    "[none] mmc0 timer oneshot heartbeat gpio default-on transient"
  )

end
